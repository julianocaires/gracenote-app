export interface Profile { id: string; user_id: string; name: string; avatar_url: string | null; theme: 'light' | 'dark' | 'system'; notifications_enabled: boolean; created_at: string; updated_at: string }
export interface Sermon { id: string; user_id: string; title: string; content: Record<string, unknown> | string; plain_text: string; preacher: string | null; cover_id: string | null; is_favorite: boolean; archived: boolean; archived_at: string | null; archived_reason: string | null; last_opened_at: string | null; created_at: string; updated_at: string; categories?: Array<{ category: { id: string; name: string; color: string | null } }>; tags?: Array<{ tag: { id: string; name: string } }>; cover?: { id: string; url: string; is_premium: boolean } | null }
export interface Category { id: string; user_id: string; name: string; color: string | null; created_at: string }
export interface Tag { id: string; user_id: string; name: string; created_at: string }
export interface Cover { id: string; url: string; is_premium: boolean; is_builtin: boolean; user_id: string | null; created_at: string }
export interface Subscription { id: string; user_id: string; is_active: boolean; plan_type: 'monthly' | 'annual' | 'lifetime' | null; expires_at: string | null; rc_customer_id: string | null; rc_original_transaction_id: string | null; created_at: string; updated_at: string }
export interface AnalyticsEvent { id: string; user_id: string; event_type: string; payload: Record<string, unknown>; created_at: string }
