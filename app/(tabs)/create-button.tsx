import { View } from 'react-native'

/**
 * Empty component used as a placeholder for the "+" tab bar button.
 * The actual navigation is handled by tabBarButton in (tabs)/_layout.tsx.
 */
export default function CreateButtonScreen() {
  return <View />
}
